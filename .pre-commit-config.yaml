repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.16.0
    hooks:
      - id: ruff-check
        args: [--fix]
      - id: ruff-format

  - repo: https://github.com/astral-sh/uv-pre-commit
    rev: 0.11.17
    hooks:
      # Keeps uv.lock in sync with pyproject.toml -- the single biggest source of
      # "works on my machine".
      - id: uv-lock

  - repo: local
    hooks:
      # RULES.md and the map TOMLs are inputs to checked-in generated code in two
      # languages. Committing one without the other is how the Python and Rust engines
      # would start disagreeing about the board itself.
      - id: gen-board
        name: board data is regenerated
        entry: uv run python tools/gen_board.py --check
        language: system
        pass_filenames: false
        files: ^(RULES\.md|ticket_to_ride/data/maps/.*\.toml|tools/gen_board\.py)$

      # The observation layout is declared once and generated into both languages.
      - id: gen-obs-spec
        name: observation spec is regenerated
        entry: uv run python tools/gen_obs_spec.py --check
        language: system
        pass_filenames: false
        files: ^(ticket_to_ride/rl/encode/spec\.py|tools/gen_obs_spec\.py)$

      # docs/CONTRACT.md is the Phase 1 -> Phase 2 gate. Drift here is never a
      # regeneration: it invalidates every replay ever recorded.
      - id: contract-vectors
        name: frozen contract vectors reproduce
        entry: uv run python tools/gen_vectors.py --check
        language: system
        pass_filenames: false
        files: ^(ticket_to_ride/engine/.*\.py|tools/gen_vectors\.py|tests/golden/.*)$

      # 84 finished games that must still be finished the same way. A rules or scoring
      # regression shows up here as a concrete failing game, not as a drifting win rate.
      - id: golden-replays
        name: golden replays still reproduce
        entry: uv run python tools/gen_replays.py --check
        language: system
        pass_filenames: false
        files: ^(ticket_to_ride/(engine|agents|data)/.*\.py|tools/gen_replays\.py|tests/golden/.*)$

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: check-toml
      - id: check-yaml
      - id: end-of-file-fixer
      - id: trailing-whitespace
      - id: check-merge-conflict
      # Not optional: the only thing standing between you and a 200MB checkpoint
      # living in git history forever.
      - id: check-added-large-files
        args: [--maxkb=1000]
